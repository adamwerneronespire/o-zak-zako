*---------------------------------------------------------------------*
*    view related FORM routines
*---------------------------------------------------------------------*
*...processing: /ZAK/IGLOG_V.....................................*
FORM GET_DATA_/ZAK/IGLOG_V.
  PERFORM VIM_FILL_WHERETAB.
*.read data from database.............................................*
  REFRESH TOTAL.
  CLEAR   TOTAL.
  SELECT * FROM /ZAK/IGLOG WHERE
(VIM_WHERETAB) .
    CLEAR /ZAK/IGLOG_V .
/ZAK/IGLOG_V-MANDT =
/ZAK/IGLOG-MANDT .
/ZAK/IGLOG_V-BUKRS =
/ZAK/IGLOG-BUKRS .
/ZAK/IGLOG_V-TDNAME =
/ZAK/IGLOG-TDNAME .
/ZAK/IGLOG_V-ADOSZAM =
/ZAK/IGLOG-ADOSZAM .
/ZAK/IGLOG_V-AS4USER =
/ZAK/IGLOG-AS4USER .
/ZAK/IGLOG_V-AS4DATE =
/ZAK/IGLOG-AS4DATE .
/ZAK/IGLOG_V-AS4TIME =
/ZAK/IGLOG-AS4TIME .
<VIM_TOTAL_STRUC> = /ZAK/IGLOG_V.
    APPEND TOTAL.
  ENDSELECT.
  SORT TOTAL BY <VIM_XTOTAL_KEY>.
  <STATUS>-ALR_SORTED = 'R'.
*.check dynamic selectoptions (not in DDIC)...........................*
  IF X_HEADER-SELECTION NE SPACE.
    PERFORM CHECK_DYNAMIC_SELECT_OPTIONS.
  ELSEIF X_HEADER-DELMDTFLAG NE SPACE.
    PERFORM BUILD_MAINKEY_TAB.
  ENDIF.
  REFRESH EXTRACT.
ENDFORM.
*---------------------------------------------------------------------*
FORM DB_UPD_/ZAK/IGLOG_V .
*.process data base updates/inserts/deletes.........................*
LOOP AT TOTAL.
  CHECK <ACTION> NE ORIGINAL.
MOVE <VIM_TOTAL_STRUC> TO /ZAK/IGLOG_V.
  IF <ACTION> = UPDATE_GELOESCHT.
    <ACTION> = GELOESCHT.
  ENDIF.
  CASE <ACTION>.
   WHEN NEUER_GELOESCHT.
IF STATUS_/ZAK/IGLOG_V-ST_DELETE EQ GELOESCHT.
     READ TABLE EXTRACT WITH KEY <VIM_XTOTAL_KEY>.
     IF SY-SUBRC EQ 0.
       DELETE EXTRACT INDEX SY-TABIX.
     ENDIF.
    ENDIF.
    DELETE TOTAL.
    IF X_HEADER-DELMDTFLAG NE SPACE.
      PERFORM DELETE_FROM_MAINKEY_TAB.
    ENDIF.
   WHEN GELOESCHT.
  SELECT SINGLE FOR UPDATE * FROM /ZAK/IGLOG WHERE
  BUKRS = /ZAK/IGLOG_V-BUKRS .
    IF SY-SUBRC = 0.
    DELETE /ZAK/IGLOG .
    ENDIF.
    IF STATUS-DELETE EQ GELOESCHT.
      READ TABLE EXTRACT WITH KEY <VIM_XTOTAL_KEY> BINARY SEARCH.
      DELETE EXTRACT INDEX SY-TABIX.
    ENDIF.
    DELETE TOTAL.
    IF X_HEADER-DELMDTFLAG NE SPACE.
      PERFORM DELETE_FROM_MAINKEY_TAB.
    ENDIF.
   WHEN OTHERS.
  SELECT SINGLE FOR UPDATE * FROM /ZAK/IGLOG WHERE
  BUKRS = /ZAK/IGLOG_V-BUKRS .
    IF SY-SUBRC <> 0.   "insert preprocessing: init WA
      CLEAR /ZAK/IGLOG.
    ENDIF.
/ZAK/IGLOG-MANDT =
/ZAK/IGLOG_V-MANDT .
/ZAK/IGLOG-BUKRS =
/ZAK/IGLOG_V-BUKRS .
/ZAK/IGLOG-TDNAME =
/ZAK/IGLOG_V-TDNAME .
/ZAK/IGLOG-ADOSZAM =
/ZAK/IGLOG_V-ADOSZAM .
/ZAK/IGLOG-AS4USER =
/ZAK/IGLOG_V-AS4USER .
/ZAK/IGLOG-AS4DATE =
/ZAK/IGLOG_V-AS4DATE .
/ZAK/IGLOG-AS4TIME =
/ZAK/IGLOG_V-AS4TIME .
    IF SY-SUBRC = 0.
    UPDATE /ZAK/IGLOG ##WARN_OK.
    ELSE.
    INSERT /ZAK/IGLOG .
    ENDIF.
    READ TABLE EXTRACT WITH KEY <VIM_XTOTAL_KEY>.
    IF SY-SUBRC EQ 0.
      <XACT> = ORIGINAL.
      MODIFY EXTRACT INDEX SY-TABIX.
    ENDIF.
    <ACTION> = ORIGINAL.
    MODIFY TOTAL.
  ENDCASE.
ENDLOOP.
CLEAR: STATUS_/ZAK/IGLOG_V-UPD_FLAG,
STATUS_/ZAK/IGLOG_V-UPD_CHECKD.
MESSAGE S018(SV).
ENDFORM.
*---------------------------------------------------------------------*
FORM READ_SINGLE_/ZAK/IGLOG_V.
  SELECT SINGLE * FROM /ZAK/IGLOG WHERE
BUKRS = /ZAK/IGLOG_V-BUKRS .
/ZAK/IGLOG_V-MANDT =
/ZAK/IGLOG-MANDT .
/ZAK/IGLOG_V-BUKRS =
/ZAK/IGLOG-BUKRS .
/ZAK/IGLOG_V-TDNAME =
/ZAK/IGLOG-TDNAME .
/ZAK/IGLOG_V-ADOSZAM =
/ZAK/IGLOG-ADOSZAM .
/ZAK/IGLOG_V-AS4USER =
/ZAK/IGLOG-AS4USER .
/ZAK/IGLOG_V-AS4DATE =
/ZAK/IGLOG-AS4DATE .
/ZAK/IGLOG_V-AS4TIME =
/ZAK/IGLOG-AS4TIME .
ENDFORM.
*---------------------------------------------------------------------*
FORM CORR_MAINT_/ZAK/IGLOG_V USING VALUE(CM_ACTION) RC.
  DATA: RETCODE LIKE SY-SUBRC, COUNT TYPE I, TRSP_KEYLEN TYPE SYFLENG.
  FIELD-SYMBOLS: <TAB_KEY_X> TYPE X.
  CLEAR RC.
MOVE /ZAK/IGLOG_V-BUKRS TO
/ZAK/IGLOG-BUKRS .
MOVE /ZAK/IGLOG_V-MANDT TO
/ZAK/IGLOG-MANDT .
  CORR_KEYTAB             =  E071K.
  CORR_KEYTAB-OBJNAME     = '/ZAK/IGLOG'.
  IF NOT <vim_corr_keyx> IS ASSIGNED.
    ASSIGN CORR_KEYTAB-TABKEY TO <vim_corr_keyx> CASTING.
  ENDIF.
  ASSIGN /ZAK/IGLOG TO <TAB_KEY_X> CASTING.
  PERFORM VIM_GET_TRSPKEYLEN
    USING '/ZAK/IGLOG'
    CHANGING TRSP_KEYLEN.
  <VIM_CORR_KEYX>(TRSP_KEYLEN) = <TAB_KEY_X>(TRSP_KEYLEN).
  PERFORM UPDATE_CORR_KEYTAB USING CM_ACTION RETCODE.
  ADD: RETCODE TO RC, 1 TO COUNT.
  IF RC LT COUNT AND CM_ACTION NE PRUEFEN.
    CLEAR RC.
  ENDIF.

ENDFORM.
*---------------------------------------------------------------------*
